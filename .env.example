# Pantheon environment configuration
# Copy this file to .env and fill in all values
# Never commit .env to version control

# OpenRouter (all LLM inference — ADK agents + tools + Muse STT fallback)
# https://openrouter.ai/keys
OPENROUTER_API_KEY=
# Optional — defaults shown in agents/openrouter_client.py / agents/model_config.py
OPENROUTER_API_BASE=https://openrouter.ai/api/v1
OPENROUTER_HTTP_REFERER=https://github.com/pantheon
OPENROUTER_TITLE=Pantheon
# Optional per-tier model overrides (defaults match former Gemini IDs via OpenRouter):
#   PANTHEON_PRO_MODEL=google/gemini-3.1-pro-preview
#   PANTHEON_FLASH_MODEL=google/gemini-3-flash-preview
#   PANTHEON_LITE_MODEL=google/gemini-3.1-flash-lite-preview
# PANTHEON_PRO_MODEL=
# PANTHEON_FLASH_MODEL=
# PANTHEON_LITE_MODEL=
# PANTHEON_MUSE_STT_MODEL=
# PANTHEON_ANALYST_MODEL=

# Amazon Nova (Bedrock)
AMAZON_NOVA_API=

# Telegram
TELEGRAM_BOT_TOKEN=

# ElevenLabs
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
ELEVENLABS_AGENT_ID=

# Hephaestus sandbox + event bus (POST /events, /sandbox/*). Hermes and agents POST events here.
#
# IMPORTANT — match *where your process runs*:
#   - `uv run python run.py` on your laptop/server host → http://127.0.0.1:9000 or http://localhost:9000
#   - Process inside Docker Compose on the `pantheon` network → http://sandbox:9000 (service name)
# Using http://sandbox:9000 on the host will fail DNS; telemetry will not reach the dashboard.
SANDBOX_API_URL=http://127.0.0.1:9000
# Legacy name; prefer SANDBOX_API_URL. Some older templates used SANDBOX_URL.
# SANDBOX_URL=http://127.0.0.1:9000

# Browser URL for the live dashboard WebSocket (Next.js `NEXT_PUBLIC_*`, baked at `npm run build`).
# Local: http://127.0.0.1:9000
# Deploy: public URL your browser can reach on /ws (e.g. http://YOUR_IP via nginx, or http://YOUR_IP:9000 direct).
# NEXT_PUBLIC_SANDBOX_URL=http://127.0.0.1:9000

# Vultr / Deployment
VULTR_SERVER_IP=
VULTR_SERVER_USER=root
VULTR_SERVER_PASS=

# Voice call Mini App (requires HTTPS for Telegram WebApps)
WEBAPP_BASE_URL=
WEBAPP_PORT=8443

# Google Cloud Run (set after running infra/cloud-deploy.sh)
GCP_PROJECT_ID=
CLOUD_RUN_REGION=us-central1
PANTHEON_AGENTS_URL=
PANTHEON_IMPACT_AGENT_URL=

# ADK A2A wiring (auto-set by cloud-deploy.sh; override for local dev)
PANTHEON_IMPACT_AGENT_CARD_URL=http://localhost:8001/.well-known/agent.json

# Optional: Slack notifications
SLACK_WEBHOOK_URL=

# Application
LOG_LEVEL=INFO
SAMPLES_DIR=/tmp/samples
ENVIRONMENT=production
